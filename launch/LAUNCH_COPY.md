# Launch Copy

## One-Liner

PREFIX for Python is a Controlled Operator Release for deterministic Python transition governance.

## GitHub Description

Deterministic Python prefix correction engine and VS Code workflow. Correct bounded invalid states locally. Refuse ambiguous ones.

## Website Hero

Govern Python transitions before they become debugging loops.

PREFIX for Python applies bounded deterministic corrections to admissible Python structure and refuses the rest for selected engineering teams in a 30-day evaluation.

In VS Code, the primary experience is Enter-triggered correction for mapped `ALWAYS_SAFE` Python block-structure states.

## Technical Subhead

Finite grammar surface. Local-first execution. No probabilistic repair path.

## Compatibility Lock

Requires CPython `3.12.x`. Validated on CPython `3.12.6`.

Python `3.11`, `3.13`, and `3.14` are not claimed in rc2.

Python `3.13` and `3.14` support requires separate AST authority catalogs.

## Announcement Post

PREFIX for Python is now live.

This is a Controlled Operator Release for selected engineering teams: a deterministic Python prefix layer and VS Code workflow that operates before execution. It corrects a bounded set of invalid states locally, verifies the result, and refuses unsupported or ambiguous paths instead of guessing.

The release includes proof-driven onboarding, CPython `3.12.x` runtime requirements, and a 30-day evaluation before paid-license conversion.

Apply for Private Operator Release access.
